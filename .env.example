# Environment variables template
# Copy this to .env ONLY if you want to override defaults
# The MCP server works out-of-the-box with production backend settings

# REQUIRED: API Authentication Token
# Copy the actual token from your secure configuration
# API_AUTH_TOKEN=Bearer your_secure_api_token_here


# BACKEND_URL=https://job-board-aggregator.onrender.com
# BACKEND_ENDPOINT=/server/match-resume-upload

# Optional: Customize rate limiting and server settings
# RATE_LIMIT_PER_MINUTE=10
# PORT=3000

# Optional: Development and debugging settings
# NODE_ENV=development
# LOG_LEVEL=info

# Optional: CORS Configuration (if needed for web interface)
# CORS_ORIGIN=*

# Optional: Analytics and Monitoring
# ANALYTICS_ENABLED=false
# SENTRY_DSN=your-sentry-dsn

# Optional: Caching Configuration
# CACHE_TTL=300

# Development/Testing Configuration
# MOCK_BACKEND=false
# TEST_MODE=false
